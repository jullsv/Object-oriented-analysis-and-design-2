# Лабораторная работа №3

**Паттерн:** посетитель (Visitor)

**Предметная область:** Разработка универсального конвертера файлов (для работы с изображениями и документами).

**Описание проблемы:**
При разработке системы конвертации файлов возникает проблема экспоненциального роста количества классов при использовании наследования.
Если для каждого типа файла (изображение, документ, видео, аудио) и каждой операции (конвертация в JPEG, PNG, PDF, MP4, MP3, валидация, публикация) создавать отдельные подклассы, потребуется огромное количество комбинаций: ImageToJPEG, ImageToPNG, DocumentToPDF, DocumentToJPEG и т.д.
Кроме того, добавление новой операции требует изменения всех существующих классов файлов, что нарушает принцип открытости/закрытости (Open/Closed Principle).
Также возникает проблема дублирования кода — логика конвертации размазана по всем классам, а проверка типов через if/else или switch делает код трудным для поддержки и расширения.

**Решение:**
Применен паттерн Посетитель (Visitor), который позволяет отделить алгоритмы от структуры объектов, над которыми они работают.
 1. Классы файлов (ImageFile, DocumentFile) отвечают только за хранение данных.
 2. Посетители (ConvertToJPEGVisitor, ConvertToPDFVisitor) инкапсулируют логику операций.
 3. Каждый посетитель реализует одну конкретную операцию для всех типов файлов.
 4. Добавление новой операции требует создания одного нового класса без изменения существующих.
 5. Добавление нового типа файла требует создания одного класса файла и добавления метода VisitNewFile() в интерфейс посетителя
 6. Принцип Open/Closed.
 7. Нет проверки типов через if/else — используется полиморфизм.

Это решение можно описать с помощью диаграммы классов:

<img width="1288" height="645" alt="image" src="https://github.com/user-attachments/assets/f81df9b4-14be-4cf2-8d47-7412a3fa9402" />

*Рисунок 1 - диаграмма классов паттерна Visitor*

Основные компоненты:

1. IFile - Базовый класс для всех типов файлов.
   
Содержит метод Accept(visitor), который принимает посетителя.
Определяет общие методы GetName() и GetSize().
Имеет поле path для хранения пути к файлу.

2. ConcreteElements

ImageFile — представляет изображение с полями width, height, format.
DocumentFile — представляет документ с полями pages, wordCount.
Каждый реализует метод Accept(), который вызывает соответствующий метод посетителя (visitor.VisitImageFile() или visitor.VisitDocumentFile()).

3. IFileVisitor (базовый класс посетителя)
   
Объявляет методы VisitImageFile() и VisitDocumentFile() для каждого типа элемента.
Содержит поле outputPath для пути сохранения результата.

4. ConcreteVisitors (конкретные посетители)
   
 - ConvertToJPEGVisitor — конвертирует файлы в JPEG (параметр quality)
 - ConvertToPNGVisitor — конвертирует файлы в PNG (параметр compression)
 - ConvertToPDFVisitor — конвертирует файлы в PDF (параметр pageSize)

5. ObjectStructure
   
Хранит коллекцию файлов.

10. Client (клиент)
Создает структуру файлов.
Применяет посетителей для выполнения операций конвертации.

### GUI

На рисунке 2 представлен интерфейс приложения (идентичный и для реализации без паттерна).

<img width="1569" height="1271" alt="image" src="https://github.com/user-attachments/assets/f80e050e-c622-4ab8-93cb-aa6335b6eb45" />

*Рисунок 2 - интерфейс приложения и демонстрация работы*

Пользователь выбирает путь сохранения конвертированных файлов. Может добавить изображение или документ, а также очистить историю файлов, и выбрать тип конвертации (PNG, JPEG, PDF). В жернале операций показана история действий с файлами.

### Реализация с паттерном Visitor

В реализации с применением паттерна Visitor логика конвертации файлов инкапсулирована в отдельных классах-посетителях (ConvertToJPEGVisitor, ConvertToPNGVisitor, ConvertToPDFVisitor), каждый из которых реализует интерфейс FileVisitor с методами visit_image_file() и visit_document_file(). Конвертация запускается через вызов structure.accept(visitor), что запускает механизм двойной диспетчеризации: каждый элемент самостоятельно вызывает соответствующий метод посетителя (visitor.visit_image_file(self) для ImageFile или visitor.visit_document_file(self) для DocumentFile). Такой подход обеспечивает соблюдение принципа Open/Closed — *добавление нового формата конвертации требует создания одного нового класса-посетителя без модификации существующих классов файлов*. Логика операций разделена по классам, что упрощает тестирование и поддержку кода, а определение типа элемента происходит автоматически через полиморфизм без явных проверок типов.

### Реализация без паттерна

В реализации без паттерна Visitor вся логика конвертации сосредоточена в едином классе FileConverter, который содержит методы convert_to_jpeg(), convert_to_png(), convert_to_pdf(). Каждый метод принимает объект базового типа File и выполняет проверку типа через isinstance(file, ImageFile) или isinstance(file, DocumentFile), после чего выполняет соответствующую ветку логики через конструкцию if/elif/else. Такой подход нарушает принцип Open/Closed — добавление нового формата требует модификации всех существующих методов конвертера, а добавление нового типа файла требует изменения каждого метода. Логика конвертации размазана по условным операторам, что усложняет поддержку и увеличивает риск ошибок при расширении функциональности. Явные проверки типов делают код менее гибким и нарушают принцип полиморфизма.
Это досадно, однако намного проще.






